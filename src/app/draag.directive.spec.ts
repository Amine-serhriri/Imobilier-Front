import { DraagDirective } from './draag.directive';

describe('DraagDirective', () => {
  it('should create an instance', () => {
    const directive = new DraagDirective();
    expect(directive).toBeTruthy();
  });
});
